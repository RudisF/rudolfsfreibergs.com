# rudolfsfreibergs.com — Claude instructions

## Content style rules

### Dashes
Always use a plain hyphen `-` instead of an em-dash `—` in all content strings,
JSX text, metadata descriptions, and copy. Never insert `—` when writing or
updating content for this project.
